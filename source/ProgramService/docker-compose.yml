services:
  database:
    image: postgres:16.13-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: programservice
      PGPORT: "5432"
    ports:
      - "5532:5432"
    volumes:
      - programservice_db:/var/lib/postgresql

volumes:
  programservice_db: